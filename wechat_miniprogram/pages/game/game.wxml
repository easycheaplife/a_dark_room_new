<!-- A Dark Room 游戏页面模板 -->
<view class="container">
  <!-- 加载状态 -->
  <view wx:if="{{loading}}" class="loading-container">
    <view class="loading-spinner"></view>
    <text class="loading-text">正在加载游戏...</text>
  </view>

  <!-- 错误状态 -->
  <view wx:elif="{{error}}" class="error-container">
    <view class="error-icon">⚠️</view>
    <text class="error-message">{{errorMessage}}</text>
    <view class="button-group">
      <button class="retry-button" bindtap="onRetry">重新加载</button>
      <button class="test-button" bindtap="onTestUrl">测试其他URL</button>
    </view>
  </view>

  <!-- 游戏内容 -->
  <web-view wx:else src="{{h5Url}}" bindmessage="onH5Message" binderror="onH5Error"></web-view>
</view>